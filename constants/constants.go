package constants

// api endpoints
const (
	STATUS_PATH = "/api/health"
	USER        = "/api/user"
	USER_ID     = "/api/user/{user_name}"
	ALL_USERS   = "/api/users"
	DOC_PATH    = "/api/swagger/docs"
)
