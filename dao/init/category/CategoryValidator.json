{
    "$jsonSchema": {
        "bsonType": "object",
        "required": [
            "categoryName",
            "collectionName",
            "author",
            "createdAt",
            "updatedAt"
        ],
        "properties": {
            "categoryName": {
                "bsonType": "string",
                "minLength": 2,
                "description": "must be a string and is required"
            },
            "collectionName": {
                "bsonType": "string",
                "minLength": 2,
                "description": "must be an string with atlease 1 character and is required"
            },
            "author": {
                "bsonType": "string",
                "minLength": 5,
                "description": "Author is required"
            },
            "createdAt": {
                "bsonType": "date",
                "description": "must be created date and required"
            },
            "updatedAt": {
                "bsonType": "date",
                "description": "must be updated date and required"
            }
        }
    }
}